name: Maven CI
on:
  pull_request_target:
    types:
      - opened
      - reopened

jobs:
  if_merged:
    if: github.event.pull_request.merged == false
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          java-version: '11'
          distribution: 'temurin'
      - run: mvn --batch-mode --update-snapshots verify jmh:benchmark
